feign:
  hystrix:
    enabled: false
    okhttp:
      enabled: true
server:
  tomcat:
    max-threads: 16
    min-spare-threads: 8
  undertow:
    io-threads: 4
    worker-threads: 24
    buffer-size: 512

#server:
#  port: 8081